# Pusher (server-side, keep secret)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=

# Pusher (public, Vite client)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=

# Vercel KV (auto-populated when you add KV store in Vercel dashboard)
KV_REST_API_URL=
KV_REST_API_TOKEN=
