{
  "name": "guess-who",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"vercel dev\" \"npm run dev --prefix client\"",
    "build": "cd client && npm install && npm run build",
    "install:all": "npm install && npm install --prefix client"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "pusher": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
